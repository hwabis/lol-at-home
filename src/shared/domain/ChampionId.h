
#pragma once

#include <cstdint>

namespace lol_at_home_shared {

// Enum values much match corresponding FB
enum class ChampionId : uint8_t {
  Garen = 0,
  Teemo = 1,
};

}  // namespace lol_at_home_shared
